name: Checks

on:
  push:
    branches: [master]

jobs:
#  build:
#    name: "Build"
#    runs-on: ubuntu-latest
#    steps:
#      - name: build
#        run: ./gradlew build
  unit-tests:
    name: "Unit tests"
    runs-on: ubuntu-latest
    steps:
      - name: tests
        run: ./gradlew test

