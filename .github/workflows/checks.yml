name: Checks

on:
  push:
    branches: [master]

jobs:
  code-style:
    name: "Code Style"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Checkstyle
        run: ./gradlew test
